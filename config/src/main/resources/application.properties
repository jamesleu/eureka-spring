spring.application.name=configserver

server.port=8888

eureka.client.service-url.defaultZone=http://localhosT:8761/eureka/
management.endpoints.web.exposure.include=*

# this can be any remote repository, or just add the config locally here in the 
spring.cloud.config.server.git.uri=https://github.com/jamesleu/eureka-spring